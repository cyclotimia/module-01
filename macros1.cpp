// #define MAX(mog, suf, ran) /* присвойте в r максимум из x и y */ ( (mog) > (suf) & (ran=mog) : (ran=suf) )

#define MAX(fuck, me, wow) {int gfy = (fuck); int aww = (me);(gfy > aww ? (wow = gfy) : (wow = aww));}

int a = 1;
int b = 7;
int c = 4;
MAX(a, b, c);
MAX(++a, ++b, c);
